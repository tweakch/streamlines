# Prototype-Werkstatt

Self-contained HTML prototypes. Everything gets tried here first — game mechanics, UI
elements, screen layouts, storytelling beats, design directions. Fiddle until happy,
then port the result "statically" into `app/`. The app never receives untested ideas.

## Rules

- Every prototype is **one self-contained `.html` file**: inline CSS + JS, no build
  step, no external dependencies beyond Google Fonts. Double-click to open, or serve
  the folder (`npx http-server prototype`) if the browser blocks `file://` features.
- A prototype does not need to be a whole game. A single screen, a nav element, a
  dialogue card, or a color/typography study is a valid prototype.
- Naming: `<topic>-v<N>[-<variant>].html`, kebab-case. New iteration on the same
  topic = new version; the superseded file moves to `archive/`.
- Prototypes are throwaway by design: global mutable state, string templates and
  copy-paste are fine here. Quality standards apply at port time, not draft time.

## Directories

### `drafts/`
Active experiments — the current best version of each topic. At most one live file
per topic. This is the only directory whose files may be edited in place.

### `ab/`
A/B comparisons when a decision needs two (or more) competing variants. One subfolder
per experiment:

```
ab/<experiment>/
  a-<label>.html
  b-<label>.html
  NOTES.md        ← the question being tested, observations per variant, the DECISION and why
```

When decided: winner moves to `drafts/` (as the topic's next version), losers move to
`archive/`. The `NOTES.md` moves to `archive/<experiment>-NOTES.md` so the reasoning
survives.

### `archive/`
Immutable history. Superseded versions, rejected variants, and drafts that were ported
land here — never edited, never deleted. Keep the version suffix and, where helpful,
add what replaced it (e.g. `-v1-squares` after the hex upgrade).

## Lifecycle

```
idea → drafts/<topic>-v1.html
     → (optional) ab/<topic>-…/ with variants + NOTES.md
     → happy? → port into app/ → move html to archive/, update ledger
     → rejected? → straight to archive/, update ledger
```

## Ledger

| File | Where | Status | Notes |
| --- | --- | --- | --- |
| `stromlinien-epoche1.html` | `drafts/` | active, partly ported | Epoche-I-Kernloop (Tag/Nacht, Plättchen, Anker-Ereignisse, Fundstellen, Zeremonie). Hex-Version (v2). Kernloop nach `app/src/stromlinien/` portiert; die App spielt seit dem Startbildschirm-Port auf dem geformten Weltkarten-Gebiet statt auf der festen 5×8-Karte des Prototyps. |
| `stromlinien-epoche1-v1-squares.html` | `archive/` | superseded | Original mit 4-seitigen Plättchen; durch Hex-Version ersetzt. |
| `map-editor-v3.html` | `drafts/` | active | Designer-Werkstatt: eine Karte, fünf Gewerke (Kartografie/Biologie/Archäologie/Geschichte/Spieldesign) als umschaltbare Linsen auf demselben Objekt. Epochen + Zeremonien wie v2, dazu editierbarer Plättchen-Katalog, Arten mit Lebensräumen, Fundstellen, Anker-/Streu-Ereignisse, Mechanik-Werte. Belegstatus + Quelle als weiche Angebote (Quellenlage-Zähler, keine Schranken). JSON v3, liest v2/v1. |
| `map-editor-v2.html` | `archive/` | superseded | Kampagnenstruktur (5 Epochen + 4 Zeremonien), nur Kartograf-Sicht. Durch v3 (Multi-Gewerk) ersetzt. |
| `map-editor-v1.html` | `archive/` | superseded | Erste Version: eine einzelne Karte (Gelände + Plättchen), ohne Epochen/Zeremonien. Durch v2 ersetzt. |
| `start-screen-v2.html` | `archive/` | **ported** (adaptiert) | Startbildschirm v2 — nach `app/src/` portiert (`StartScreen.tsx` + `world.ts`). Bei der Portierung wurde die **Seed-Welt durch die gestaltete Weltkarte ersetzt** (Entscheidung: gestaltete Welt mit realen historischen Fakten; Alpenrhein Landquart–Konstanz, 22×44 Hexes, Fundstellen/Zeichen/Landmarken an festen Weltkoordinaten). Nebel des Ungespielten, formbares Gebiet (12–55 Felder, zusammenhängend), Zeichen ◈, Epochen-Ausblick und Startbedingung (≥1 Flussfeld) wurden 1:1 übernommen; das geformte Gebiet wird an den Kernloop übergeben. |
| `start-screen-v1.html` | `archive/` | superseded | Erste Version: sichtbares Gelände, fester 7×9-Rechteckrahmen. Durch v2 (Nebel, formbares Sechseck-Gebiet, Zeichen, Epochen-Ausblick) ersetzt. |
| `mechanik-labor-v1.html` | `drafts/` | active | Monte-Carlo-Prüfstand für Balancing: headless Port des Epoche-I-Kernloops, komplett datengetrieben. Editierbar: Karte (Gelände, Furten, Fundstellen, Start-Plättchen), Deck-Zusammensetzung, Plättchen-Werte, Verbünde, Arten (Flora/Fauna mit Lebensräumen, Standard aus), Technologien, Anker-/Streu-Ereignisse, Regeln. Bot spielt N Läufe (Strategien: ausgewogen/Nahrung/Schutz/Auth/Zufall, seedbar); Auswertung: Überlebensquote, Sesshaftigkeits-Histogramm mit Tier-Schwellen, Ressourcen-Verlauf, Ereignis-Bilanz, Belegungs-Heatmap, Einzellauf-Zeitleiste. Basis pinnen → Deltas nach Tweaks. JSON-Export/Import, localStorage. |
| `nacht-effekte-v1.html` | `drafts/` | active | Werkstatt-Prüfstand: Was passiert, wenn die Nacht **bleibende Zustände** statt Einmal-Effekte hinterlässt? Zeitweilige Effekte auf Feldern mit Restdauer (Wild zieht weiter, Schneebruch, Verlandung, Wildseuche, Hochwasserschlamm), davon drei mit **Ausbreitung** je Nacht (Wasser / Land / Ufer). Kern des Versuchs: Der Spieler **wählt selbst, welches Feld den Effekt trägt** (Kandidaten = die ertragreichsten) – Anwenden als Entscheidung statt Buchhaltung. Gegenmaßnahme „Beheben" −2 Material; der **Jäger ➤** blockt Ausbreitung von seinem Feld, die **Sammlerin ✦** bringt +1 Ertrag (auf befallenem Feld nichts). **Wanderer** zieht übers Brett aufs Lager zu → Aufnehmen (+Kultur/+Material, Seuchenrisiko) oder Abweisen (−Nahrung bei schwachem Schutz). Effekte bewusst natürlich, nicht übernatürlich (Epoche I fürchtet das Tier). Debug: `?autostart`, `?demo`, `?night`. |
| `ereignis-labor-v1.html` | `drafts/` | active, **teilweise ported** | **Prüfstand für gescriptete Ereignisse im Rundenablauf** — Ereignisse hängen hier nicht an Runden, sondern an **Ort, Geometrie, Zeit und Abschnitt**. Bühne ist die gestaltete Weltkarte (22×44, 1:1 aus `app/src/stromlinien/world.ts`) samt einer Beispiel-Siedlung (16 Plättchen, Sammlerin ✦ und Jäger ➤), damit Ereignisse etwas treffen, das dem Spieler gehört. **Die Runde läuft in den vier Abschnitten des Kernloops** (Morgen · Tag · Nacht · Rundenende, wie `engine.ts`), und jede Ereignisphase hat darin ihren Platz: Vorzeichen am Morgen, Angebot am Tag, Einschlag in der Nacht (ersetzt die Streu-Nacht), Ausbreitung in den Folgenächten (begleitet sie), Nachwirkung am Morgen danach, Spur dauerhaft. Ansicht **Spielsicht** zeigt Abschnitt für Abschnitt, was der Spieler liest und entscheidet. **Handlungsfenster**: je Ereignis zwei benannte Antworten mit Preis, wirksam beim Einschlag — sie greifen an einer *Kostenregel* der Ausbreitung an (römische Dämme = kein Sprung vom Bett aufs Ufer: 294 → 232 veränderte Felder, 9 → 5 verlorene Plättchen). Sieben Ausbreitungsarten als Kostenfunktionen über einer Suche: Punkt, Radius, **Korridor** (folgt dem Flussbett talwärts, auch dem trockenen), **Uferlinie**, **Diffusionsfront** (Talsohle schnell, Hang zäh, mehrere Quellen treffen sich), **Feuer** (nur über Wald, Wind gibt die Richtung), **Pegel**, dazu „ganzes Tal" für Klima ohne Ort. **Ketten**: ein Ereignis löst ein anderes aus (Felssturz → trockenes Bett → Stausee bricht) — und das Vorzeichen eines Ereignisses darf ein anderes Ereignis sein: wer in Runde 3 ins trockene Flussbett geht, verliert in Runde 5 Menschen darin. Zwei Drehbücher: Epoche I (Wiederbewaldung, Flimser Bergsturz mit Ursache abseits der Karte, Ausbruch des Bergsturzsees, Blitz-Waldbrand, 8.2-ka-Kälte, Pfahlbauten am Seeufer, Seespiegelanstieg) und Epoche II (Höhensiedlungen, Rom als Diffusionsfront aus zwei Richtungen, Talstraße, Hochwasser), je Eintrag mit Belegstatus + Quellentext. **Der Waldbrand wählt seinen Ort zur Laufzeit** (trockenster Wald im Talboden) — gescriptet in der Zeit, emergent im Raum. **Spuren-Ansicht** zeigt, was das Drehbuch dauerhaft umgeschrieben hat: das Drehbuch ist auch ein Kartengenerator. Regie-Panel mit Reglern und umschaltbarer Ausbreitungsart + Live-Vorschau. Ressourcen rechnet es bewusst nicht (das tut `mechanik-labor-v1`). Debug: `?buch=e1\|e2`, `?runde=N`, `?slot=morgen\|tag\|nacht\|schluss`, `?ev=<id>`, `?vorzeichen=<id>` (hält im Tag mit offenem Handlungsfenster), `?antwort=<id>:<n>,…` (Antworten vorwählen, für Vergleiche ohne Klick), `?tab=spielsicht\|drehbuch\|regie\|protokoll`, `?siedlung=aus`, `?autostart`, `?demo`, `?tempo=langsam\|normal\|schnell`, `?night`, `?spuren`. | **In die App portiert (21. Aug 2026): die Dramaturgie ohne Geometrie.** Die vier Anker in `app/src/stromlinien/data.ts` tragen jetzt `vor`/`vorT`/`nachT`/`antworten`; der Morgenbericht im Spiel zeigt Vorzeichen und Nachwirkung, das Handlungsfenster steht von der Vorzeichen-Runde bis zum Tag des Einschlags offen, und `fx` liest die Wahl aus `state.antwort`. Belegt am Ergebnis: dieselbe Runde, andere Wahl — die grosse Kälte kostet 4 Nahrung ohne Vorbereitung und 2 mit Vorräten; der Pfahlbau-Anker gibt 1, 2 oder 3 Plättchen. **Nicht portiert** (bewusst, siehe Handbuch): Ausbreitung, Geländespuren und Verluste an Plättchen/Menschen — das gehört vorher durch das Mechanik-Labor.
| `kartenwachstum-v1.html` | `drafts/` | active | **Die Welt wächst mit den Wegen — Gegenentwurf zu `zoomstufen-labor-v1`.** Die Dichte bleibt in *jeder* Epoche gleich (Ebene 1, 2 km/Hex, 3,5 km² je Feld); grösser wird nicht das Feld, sondern die **Zahl der bekannten Felder**. Bekannt ist, was begangen wurde: von jeder Gruppe läuft eine Kostensuche über das echte Gelände (Flachland 2 · Hang 5 · Bergland 14 km-Äquivalent; Fluss ohne Boot 12 = Sperre, mit Einbaum 1,0, mit Plankenboot 0,6 — **erst wenn Wasser billiger ist als Land, entstehen Korridore**). Zwei Budgets je Epoche, weil zwei verschiedene Dinge wachsen: **heim** (das Gebiet, das im Jahreslauf abgelaufen wird → Fläche) und **fern** (wie weit Wege zu *anderen Orten* reichen → Linien mit Sichtstreifen). Ziele sind die 23 echten Landmarken des Datensatzes. **Sicht folgt dem Tal, nicht dem Zirkel**: Breitensuche durch offenes Gelände, Bergland wird gesehen aber nicht durchschaut. **Nebel des Vergessens** als dritter Zustand neben bekannt/nie gesehen: was verlassen wird, fällt zurück in Nebel und behält nur seine Umrisse — Klick auf die Karte zieht die Gruppe weg und das alte Tal verblasst sofort. Das Dorf bleibt liegen, auch im Nebel. **Gemessene Kurve** (gleiche Ausgangslage, Landquart): Mesolithikum 420 Felder / 1 455 km² → Neolithikum **112 / 388** → Bronzezeit 1 569 / 5 435 → Eisenzeit 3 454 / 11 965 → Römerzeit 3 359 → Mittelalter 3 360 → Neuzeit 3 365. Drei Befunde: (1) **die Kurve fällt beim Sesshaftwerden** um 73 % — Bauern kennen weniger Welt als Jäger, und das Modell trifft dabei die ethnographischen 100–2 000 km² für Wildbeuter-Streifgebiete; (2) **die Römerstrasse senkt die bekannte Fläche leicht** (−95 Felder), weil sie Wege verkürzt — Infrastruktur verbindet Orte und ent-kennt den Raum dazwischen; (3) ab der Eisenzeit **sättigt das Modell**, weil alle 23 Ziele erreichbar sind — Wachstum über Wege braucht eine Siedlungsschicht, die die Pipeline nicht hat. Debug: `?epoche=0..6` (läuft die Epochen der Reihe nach, damit „vergessen" echt entsteht), `?ort=landquart\|chur\|rheindelta`, `?sicht=1..12`, `?vergessen=aus`, `?night`; ←/→ wechseln die Epoche, `＋ Gruppe teilen` setzt eine zweite Sippe. |
| `zoomstufen-labor-v1.html` | `drafts/` | active | **Der Epochenübergang als Zoomwechsel, auf echten Daten** — Konsument von `rhein-tiles-v4.data.js`: nicht die Karte wird grösser, das Feld wird kleiner. Drei echte Ebenen (10 → 2 → 0.4 km/Hex), eine bespielte Beispiel-Siedlung, Aufbruch-Animation (jedes Kind startet im Mittelpunkt seines Elternfelds), gestrichelter Umriss des alten Spielfelds nach dem Wechsel. **Startebene ist 1, nicht 0**: die gestaltete Weltkarte der App (22×44 Felder auf ~90 km Tal) liegt bei ~4 km/Hex, Ebene 0 (10 km/Hex) ist Kampagnen-Übersicht und keine Spielebene. Zwei Kameras zeigen die eigentliche Entscheidung: **Land halten** (derselbe Ausschnitt, die Felderzahl explodiert — 525 → 12 221 im Blick) gegen **Felderzahl halten** (der Ausschnitt schrumpft 5× — man spielt danach in einem *Bruchstück* des alten Tals, ein altes Feld füllt den halben Bildschirm). Die bespielte Karte ist in **km-Weltkoordinaten** verankert, nicht in Zellindizes — nur darum übersteht sie den Wechsel; dieselbe Regel, nach der `world.ts` Fundstellen an Weltkoordinaten bindet. **Gemessene Befunde:** die Pipeline springt je Ebene um Faktor 5 in der Länge, also **25× in der Fläche** — ein Feld zerfällt in ~25 Kinder, nicht in 7; und weil Hex-Raster sich **nicht exakt verschachteln**, schwankt die Kinderzahl je Feld (gemessen 21–27, Mittel 25.07 über 140 Felder). 16 bespielte Felder werden zu einem Spielfeld von 401, die Siedlung sitzt danach auf 4.0 % davon. Die feinere Ebene ist **kein Interpolat**: dieselbe Stelle bei Chur ist auf Ebene 0 „Fluss", auf Ebene 1 „Ufer", auf Ebene 2 „Flachland" — der Zoom fügt echtes Wissen hinzu, statt Vorhandenes zu unterteilen. Debug: `?ebene=0\|1\|2`, `?kamera=land\|felder`, `?ort=chur\|landquart\|rheindelta\|konstanz`, `?siedlung=aus`, `?night`; Klick verschiebt die Mitte, ←/→ wechseln die Ebene. |
| `stromlinien-handbuch.html` | `drafts/` | active, **knowledge base** | Handbuch & Designdokument — die Wissensbasis des Projekts. Zwei Ansichten: **Publish** (`?mode=publish`, nur Umgesetzt + Konzept) und **Entwicklung** (alles inkl. Ideen, Verworfenem, offenen Fragen). Bei Design-Entscheidungen hier den Status nachführen. |
| `stromlinien-technik.html` | `drafts/` | active, **knowledge base** | **Technik-Handbuch: Renderpipeline** — das technische Gegenstück zum Handbuch. Wie gemessen wird (headless Chrome über das DevTools-Protokoll mit **echter Uhr**; `--virtual-time-budget` taugt nicht, dort steht die Uhr während synchroner Arbeit, und `?bench` gilt nur im echten Browser), dazu der Pixel-Vergleich zweier Screenshots als 10×10-Dichtekarte, mit dem sich ein vermeintlicher Schraffur-Fehler als Fehllesung erwies. Der Befund: 28 576 Vektorhexe für 2.6 px Radius, ein grosser Pfad ist **langsamer** als 54 kleine (1 224 gegen 549 ms), Batch-Schlüssel als Zeichenkette in einer Map, Basis-Cache an der Kamera — und das Modell war unschuldig (unter 7 ms pro Bild). Dann die zwei Betriebsarten, der Zellbild-Trick mit der doppelten Bitmap-Breite (der Halbzellen-Versatz der odd-r-Zeilen ist genau ein Bildpunkt), Farbtabellen gegen Drift zwischen den Renderpfaden, die Vorher/Nachher-Bilanz, die Skalierung auf ein feineres Raster (Zeichnen skaliert mit **Bildschirmpixeln statt Zellen**; es bleiben Stützstellen-Speicher, Lauf im Hauptthread und der Bake als Decken), sieben **Regeln für neue Zeichenpässe** und die offenen Hebel. **Neu (20. Aug 2026): `eiszeit-labor-v3` vollständig vermessen** — die 31.6 s Seitenaufbau zerlegt (1.1 s alles ausser dem Lauf, 30.8 s der Lauf, davon 12.3 s Eisstauseen und 5.4 s die 27 Gleichgewichte), **325 MB Heap** davon 226 MB Stützstellen (`?key=1000` halbiert das ohne Zeitverlust), Zellbild 14 ms gegen `?raster=hex` 12.2 s warm / 110.9 s kalt (die Hochrechnung „~4 800 ms" war zu freundlich) — und **zwei Bedienungsbremsen ausserhalb des Renderers**: der Eisstausee-Flood läuft je *angezeigtem* Bild (60 ms, fünfmal das Kartenbild; Abspielen 7.9 statt 52.6 fps), und ein Sprung **vorwärts** auf der Zeitleiste spielt den Lauf nach statt die Stützstelle zu laden (9.2 s statt 0.4 s bei nachweislich gleichem Zustand). **Kap. 11 „Der Weg auf grössere Karten" (21. Aug 2026):** derselbe Prüfstand auf v2 *und* v3 zeigt **flache Kosten je Feld** (Zeitschritt 56 → 53 ns, Gleichgewicht 637 → 624 ns bei 12.4× Karte — nur der Priority-Flood wird je Feld 30 % teurer, das ist der Heap), daraus eine **Kostengleichung**, die den Lauf auf 10 % trifft (63 % Zeitschritte, 20 % Flutungen, 17 % Gleichgewichte). Der Befund darin: ein feineres Raster kostet **k³, nicht k²**, weil `effFront = min(vFront, hexKm·1000/dt)` die Front auf ein Feld je Schritt festnagelt — der Ausweg (Überschuss ins nächste Feld weitertragen) ist bei 1 km Byte für Byte folgenlos, weil `vorStep` dort exakt 1.0 ist. Dazu die gemessenen Faktoren: Flutung nur bei Bedarf (0.26–0.46 % der Felder ändern sich je 250 a), aktive Menge im Zeitschritt (2.8–31 % ändern sich), Zählsortierung statt Vergleichssort (66.9 → 1.7 ms), Stützstellen ohne `ABTRAG` und gepackt (226 → 27 MB) — und die Untergrenze, die sagt, wann Schluss ist: ein 6-Nachbar-Durchgang über N kostet 6.6 ms, der Zeitschritt 18.8 ms, also bringt Mikrooptimierung dort höchstens 2×. |
| `klimatile-stilguide-v1.html` | `drafts/` | active | **Stil-Guide-Template + Master-Set-Probe**, erster Umsetzungsschritt aus `art-direction-handbuch.html` Kap.&nbsp;7. Quelle: `pipeline/assets/climate-tiles/` — eigenständige SVG-Dateien (noch nicht an Bake/Bundle angebunden): `_template.svg` (sechs benannte Layer `base-terrain/vegetation-primary/vegetation-secondary/water-features/climate-indicators/human-traces` + `edge-mask`, Palette als lokale `:root`-CSS-Variablen je Datei) und drei ausgearbeitete Master-Set-Kombinationen — `atlantikum-mitteleuropa` (dichter, lichtdurchfluteter Wald, bewusst ohne menschliche Spuren), `subboreal-pontisch` (Steppen-Wald-Mosaik mit Weidepfad + angedeutetem Feldstück), `subatlantikum-nahost` (trockenes Wadi, dem Vegetation und Siedlungspunkte sichtbar folgen). Der Prototyp lädt Template-Quelltext und Tiles per `fetch()` über den laufenden `pipeline/asset-server.mjs` (liest beliebige Repo-Dateien, kein neuer Endpunkt nötig), zeigt jede Kombination bei 32/128/256&nbsp;px (Kap.&nbsp;1 Skalierbarkeit) und macht jeden der sechs Layer einzeln abschaltbar. Offen: die restlichen 5–7 Master-Set-Kombinationen, zweite Detailstufe pro Tile, Übertragung auf den bestehenden Terrain-Asset-Bestand. Debug: `?server=<url>`, `?tile=<dateiname>`. |
| `art-direction-handbuch.html` | `drafts/` | active, **knowledge base** | **Art-Direction-Handbuch: visuelle Sprache der Terrain-Tiles** — drittes Wissensdokument neben Handbuch und Technik-Handbuch, aus dem Art-Director-Briefing (21. Aug 2026) in acht Kapitel übersetzt: technische Grundregeln (Format/viewBox, Skalierbarkeit, Farbraum als CSS-Variablen, sechsteilige Layer-Struktur `base-terrain → vegetation-primary → vegetation-secondary/water-features → climate-indicators → human-traces → edge-mask`, Hex-Kanten), die inhaltliche Matrix (fünf Holozän-Stufen Preboreal/Boreal/Atlantikum/Subboreal/Subatlantikum × acht Klimaregionen Europa/Naher Osten), visuelle Sprache (Stil, Farb-/Stimmungslogik pro Epoche, Klimaregions-Signaturen, Vegetations-Hierarchie, Fauna als Spur), menschliche Spuren (extrem zurückhaltend), Konsistenz-/Matching-Regeln (gemeinsame Bodenfarbe je Klimaregion, feste Lichtrichtung, Übergangs-Bibliothek an den Kanten), emotionale/narrative Qualität, Lieferumfang (Master-Set zuerst, drei Detailstufen, Stil-Guide-Template) und ein abschliessendes Kapitel **Abgleich mit dem Bestand**: der bestehende Terrain-Asset-Bestand (`pipeline/assets/terrain/`, sieben Kollektionen aus `asset-editor-v1`) erfüllt nur die Format-Regel vollständig, der Rest — Layer-Struktur, Klimaregion-Achse, Vegetations-Hierarchie, Kanten-Matching, Master-Set/Detailstufen/Template — fehlt noch; die drei Epochen-Kollektionen (`palaeolithisch/neolithisch/holozaen`) sind nur ein grober dreistufiger Vorläufer der hier verlangten fünfstufigen Chronologie und sollten bei einer Überarbeitung eher ersetzt als erweitert werden. Bestehende Tiles sind dort als Bild eingebunden, wo sie zur jeweiligen Vorgabe passen (Kap. 1 Format, Kap. 3 Epochen-Stimmung, Kap. 4 menschliche Spuren als Gegenbeispiel). |
| `spielmenue-v1.html` | `archive/` | **ported** | Session-Lifecycle-Details, vollständig in die App übernommen. Mit der Shell kamen Lager-Menü, Aufgeben-Confirm, überspringbare Nacht-Sequenz und der Autosave selbst; **21. Aug 2026 nachgezogen**: der **Autosave-Indikator** (Punkt im Spielkopf, Wortlaut im Lager-Menü — im Kopf verdrängte der Text die Jahreszahl) und die **Fehlerfall-Karte** als React `ErrorBoundary` (`app/src/shell/ErrorBoundary.tsx`) mit „Aus Autosave wiederherstellen" / „Neue Partie beginnen"; sie nennt Gebiet, Runde, Phase und Alter des letzten Stands, statt Verlustfreiheit nur zu behaupten. Prüfbar über `?dev=fehler` — der Prototyp hatte dafür einen Menüeintrag „Fehlerfall testen". |
| `shell-v2.html` | `archive/` | **ported** | **Die Shell** (Meta-UI rund ums Spiel) — nach `app/src/shell/` portiert (Titelbild, Klan gründen, Epochen, Regeln, Über + Router in `App.tsx`; Weltkarte = erweiterter `StartScreen`, Spiel mit ⬡/☰). Kernideen im Port: Resume-Karte mit „zuletzt:"-Kontext auf Titel und Weltkarte, Kampagne als Info-Station, Menü von rechts, Autosave nach jeder Aktion (`shell/storage.ts`, Schema-versioniert), Nebel pro Profil. Gemessen im Prototyp (420×860): erstes Spiel 3 Klicks / 72px (v1: 5 / 1109px). **Nicht portiert:** Chronik-&-Sync-Screen (braucht erst den Endpunkt, siehe `profil-hub-v2`), globale Enter/Esc-Tastatursteuerung. |
| `shell-v1.html` | `archive/` | superseded | Erste Fassung der Shell: Hub und „Gebiet formen" noch getrennte Screens, Kampagne im Pflichtweg, Menü fuhr von links unter einem Auslöser oben rechts ein. Durch v2 ersetzt (kürzere Wege, Weltkarte als Hub). |
| `profil-hub-v2.html` | `drafts/` | active | Profil, Hub **und Chronik-Sync**. Erweitert v1 um die Cloud-Anbindung: **Sync ist opt-in** (Standard aus), Identität über einen **Chronik-Code** (Wortkette, kopierbar als Code oder Link, `?chronik=CODE` füllt den Holen-Dialog vor), Konflikt-Dialog „Zwei Chroniken" (Gerät vs. Cloud, Runde + Zeitpunkt, eine Seite gewinnt), Offline-/Fehlerzustand, angepasster Erststart-Text („zunächst nur auf diesem Gerät"). Der Server ist durch einen zweiten `localStorage`-Schlüssel simuliert, damit der Rundlauf **sichern → Gerät leeren → holen in einem Browser** testbar ist. Debug: `?empty`, `?demo`, `?sync`, `?restore`, `?conflict`, `?offline`. |
| `profil-hub-v1.html` | `archive/` | superseded | Erste Version: lokales Profil, Profil-Umschalter, Hub-Karte für laufende Partien, Chronik-Export/Import als Datei — aber ohne Cloud-Sync. Durch v2 ersetzt (Chronik-Code, Konflikt, Opt-in-Sync). |
| `feld-labor-v1.html` | `drafts/` | active | **Anatomie eines Feldes** + der **Feld-Explorer**. Prüfstand für die Frage, wie ein Hexfeld aussieht, wenn es über die Epochen Landschaft, Plättchen, Menschen, Flora/Fauna, Ereignisse und Kanten-Mods gleichzeitig tragen muss: Mittelfeld + 6 Nachbarn, jedes Merkmal per Werkbank an/aus. Ergebnis-Modell: **Schichten** (Grund → Kern → Kanten → Slots) mit **festen Slots** (Kern Mitte · Krone oben rechts Menschen · Wange oben links Ereignis+Restdauer · Ferse unten links Arten · Fuss unten Mitte Zeichen ◈ · Zahl unten rechts Feldwert · Kanten sechs Seiten) und **Detailstufen** S/M/L nach Hexbreite (<52 / 52–75 / ≥76px), die Slots wegnehmen statt zu schrumpfen. Marken sind in `em` bemessen und skalieren mit `--hexw`; Slots liegen auf einem Ring bei ~22% (die Boxecken des Spitz-oben-Sechsecks sind leer); Kanten-Balken sitzen mittig auf der Kantenmitte, die Gegenkante wird mitgesetzt. Der **Feld-Explorer** ist nicht-diegetisch (Mono-Register, „Werkzeug"), mobil Vollbild, ab 900px Seitenpanel, das das Brett schmaler macht (und damit die Detailstufe sichtbar senkt) — mit Hero-Hex, Abschnitten und **Feldwert-Ledger** (jede Zeile mit Quelle: Plättchen, Verbund, Art, Mensch, Ereignis) statt einer nackten Zahl. Epochen-Vorlagen I–V zeigen dieselbe Zelle mit wachsender Last; „Volllast" als Stresstest. Debug: `?epoche=1..5`, `?explorer`, `?max`, `?lod=s|m|l`, `?size=42`, `?slots`, `?night`, `?feld=1,0`, `?leer`, `?demo`. |
| `gewaesser-labor-v1.html` | `drafts/` | active | **Gewässer als Band im Feld statt als gefärbtes Feld.** Ausgangsbefund: ein Hexfeld ist 1 km breit, ein Bach 3 m — ein Feld „Fluss" zu färben rendert einen 1 km breiten Fluss. Hier fliesst das Wasser deshalb **von Kantenmitte zu Kantenmitte durch** das Feld (quadratische Kurve über die Feldmitte), womit die drei Formen der Aufgabenstellung von selbst entstehen: **gerade** (Gegenkanten), **gebogen** (120°), **geknickt** (60°). Weil benachbarte Felder dieselbe Kantenmitte teilen, ist der Lauf über Feldgrenzen hinweg stetig. Damit **wandert das Ufer ins Flussfeld**: was neben dem Band bleibt, ist Land — eigene Uferfelder entfallen. Zweite Kernidee: **die Benennung wird gerechnet, nicht gemalt.** Aus den Läufen entsteht ein gerichteter Graph über Knoten (Feld bzw. Stillwasser-Gruppe), darauf läuft eine Abflussbilanz in topologischer Reihenfolge; die Breite folgt der hydraulischen Geometrie **b = 6·√Q** (kalibriert: Alpenrhein-MQ 90 m³/s → 57 m, Rhein bei Basel 1050 → 194 m), und aus der Breite fällt die Klasse. Die komplette Systematik ist damit abgedeckt und immer widerspruchsfrei — **Rinnsal · Bach · kleiner Fluss · grosser Fluss · Strom** (natürlich, Schwellen 0,5 / 5 / 10 / 100 m; „Strom" nur, wenn das Wasser die Karte Richtung Meer verlässt), **Kanal** (künstlich: Breite gebaut, nicht gewachsen, mit **Ausleitung** — der Mühlkanal entnimmt 1,4 m³/s und stuft den Hauptfluss darunter sichtbar von *grosser* auf *kleiner Fluss* zurück, unterhalb der Rückgabe wieder hinauf), **Pfütze · Tümpel · Weiher · See** (aus Fläche × Dauerhaftigkeit) und **Teich · Stausee** (dieselben Schwellen, Herkunft künstlich; der Stausee entsteht nicht als Malfläche, sondern aus dem **Damm**: flussaufwärts fluten bis zum Stauziel). Bach + Bach = kleiner Fluss ist damit keine Regel, sondern ein Ergebnis. **Merkmale werden aus dem Gelände abgeleitet** (Stempel überschreibt, dreistufig an/aus/automatisch): **Wasserfall** ab 120 m/km, **Stromschnelle** ab 55, **Klamm** wo ≥4 Flanken 150 m über dem Bett stehen (mit 80 m war jedes Bergbachfeld eine Klamm), **Verzweigung/Schotterflur** wo es flach und breit genug ist (dreifache Gerinnebreite, drei Rinnen, die sich an den Kanten wieder treffen), dazu **Brücke · Steg · Damm** von Hand. **Passierbarkeit läuft von Ufer zu Ufer, nicht von Feld zu Feld:** k Wasserkanten ergeben k Strahlen zur Feldmitte und damit genau k **Landsektoren** — jeder ist ein eigener Knoten im Wegegraph, benachbarte Felder verbinden sich über gemeinsame Zugangspunkte (halbe Kanten dort, wo Wasser die Kante zerschneidet), sodass „links bleibt links". Ein Quellfeld hat nur einen Strahl und trennt nichts — um eine Quelle geht man herum. Der Mensch mit **Reichweite 6** läuft darauf einen Dijkstra, zweimal: einmal ohne, einmal mit Wagnis; die Karte zeigt erreichte Sektoren mit Restpunkten, riskant erreichte mit Überlebenswahrscheinlichkeit. Kosten in **zwei umschaltbaren Modellen**: die **Klassentabelle** (Bach −1, kleiner Fluss −2, grosser Fluss −3, Strom ✕, Furt −1, Stromschnelle −5 bei 50 %, Wasserfall/Klamm ✕, Brücke/Steg 0) und die **Hydraulik** — Tiefe und Geschwindigkeit aus Manning (t = (Q·n/(b·√J))^0,6), Kosten aus **t·v**, dem üblichen Watsicherheits-Mass. Die Hydraulik sagt, was die Tabelle nicht kann: ein knietiefer Wildbach ist gefährlicher als ein hüfttiefer träger Fluss. **Zwei Befunde aus dem Bauen:** (1) Eine Furt absolut zu definieren (t·v unter Schwelle) machte *fast jedes Feld* zur Furt — rechnerisch richtig, spielerisch wertlos; eine Furt ist ein **Ort**, also zusätzlich ein lokales Minimum von t·v gegen die vier Nachbarfelder im Lauf. Ihre Lieblingsstelle ist die verzweigte Strecke, weil sich dort dasselbe Wasser auf die dreifache Breite verteilt — womit die historischen Furten (Schaan, Tardis) genau dort entstehen, wo sie real lagen. (2) Der **Maßstabsschalter** (lesbar / halb / echt) führt den Ausgangsbefund vor: maßstabsgetreu verschwindet alles unterhalb eines Stroms zu Haarlinien; im Inspektor steht je Feld der Überzeichnungsfaktor und „Wasser nimmt 4,7 % der Feldbreite ein — je Seite bleiben 477 m Ufer im selben Feld". Editor im Prototyp: Läufe zeichnen (natürlich/Kanal, Quellschüttung), Merkmal-Stempel, Stillwasser setzen, Höhenanker, Löschen (schneidet Läufe auseinander), Mensch setzen. Demokarte 15×18 mit allen zwölf Klassen gleichzeitig. Debug: `?demo`/`?leer`, `?feld=c,r`, `?blick=c,r,felder`, `?wanderer=c,r`, `?kein-mensch`, `?range=6`, `?massstab=lesbar\|halb\|echt`, `?modell=klasse\|hydraulik`, `?km=1`, `?fluss`, `?seiten`, `?ufer=0`, `?merkmale=0`, `?tab=feld\|katalog\|regeln\|wand`, `?tool=`, `?hi=gfluss`, `?panel=0`, `?night`. |
| `ab/gewaesser-kacheln/` | `ab/` | active, **A/B offen** | Frage: sieht `gewaesser-labor-v1` hübscher aus mit echten Textur-Kacheln statt Flächenfarbe für Flachland/Aue/Stillwasser? **a-vektor** (Referenz, unverändert) gegen **b-kacheln** (Grund-Texturen aus dem CC0 „Hex Tileset Pack", adythewolf/OpenGameArt — nur die Flächenfüllung ersetzt, nicht die Hex-Kontur, da das Pack flat-top statt pointy-top ist; der prozedurale Fluss bleibt in beiden identisch). Entscheidung noch offen, siehe `NOTES.md`. |
| `eiszeit-labor-v3.html` | `drafts/` | active | **Der ganze Alpenbogen — und zwei falsifizierbare Eingriffe ins Eismodell.** Region von 7.95–10 °O (Linth + Alpenrhein) auf **4.7–14.9 °O / 44.9–48.6 °N** geweitet: von den Endmoränen des Rhônegletschers in der Dombes bei Lyon bis zum Murgletscher bei Judenburg, von den Amphitheatern der Po-Ebene bis an die Donau. **744×475 = 353 400 Felder** à 1 km aus **fünf** Sonny-DTMs (FR/CH/IT/AT/DE; FR im Register auf `fuer: eiszeit` gesetzt), Datenfile 3.7 MB. 60 Landmarken statt 19, davon **11 △-Kalibriermarken** in fünf Ländern (Dombes, Wangen a. d. Aare, Killwangen, Schaffhausen, Ivrea, Verbano, Brianza, Villafranca, Memmingen, Salzachlappen, Judenburg) und **7 Gegenproben, die eisfrei bleiben müssen** (Lyon, Basel, Mailand, Turin, Verona, München, Ulm) — erstmals stehen Prüfpunkte *ausserhalb* des Gebiets, an dem das Modell justiert wurde. Landmarken haben einen `rang`, weil sich 60 Beschriftungen auf 740 km Kartenbreite sonst gegenseitig überdecken (`?rang=1` erzwingt die kleine Auswahl). Wo keine Quelle deckt (Slowenien, Nordadria) wird **nicht mehr blind geschätzt**: gefüllt wird nur bis `fuellMax` Felder weit, der Rest trägt **Bit3 „leer"** (Höhe 0, ausserhalb der Modellfläche — kein Eis, kein Fliessweg, in keiner Kennzahl; 20 375 Felder, 5.8 %). Grund: ein 15 000 km² grosses Loch mit nächster-Nachbar-Füllung schmiert die Julischen Alpen als glattes Hochplateau nach Süden und erfindet dem Eis ein Nährgebiet. **Neu 1 — bettabhängige Fliessgrenze.** τ ist ein Feld statt einer Zahl: Talboden/Becken 50 kPa, Fels 98, entschieden aus dem Relief über dem lokalen Talboden und der Rauheit in der Zelle (`?bettkarte` zeigt es — Po-Ebene, bayerisches Vorland, Mittelland und Rhône-Korridor ocker, das Massiv blaugrau). **Gemessen hat das allein das umgekehrte Vorzeichen**: 163 251 → 186 776 km², und über Turin (1 917 m), Mailand (994 m) und Basel (629 m) stand Eis, wo keines stehen darf. Der Dijkstra trägt die Oberfläche vom Nährgebiet nach *aussen* — ein kleineres τ hält sie flacher und damit länger hoch, also wird das Eis über der tiefen Po-Ebene dicker, nicht dünner. „Weiches Bett → flacher Lappen" gilt vom **Rand** her, deshalb steht τ jetzt zusätzlich als **plastischer Deckel** H ≤ √(2τx/ρg) mit x = Abstand zum Eisrand, in zwei Bilanz-Durchgängen (der Deckel senkt die Oberfläche, das ändert die Massenbilanz, und eine gekappte Zunge hört von selbst auf zu wachsen) und **ausgenommen eingezwängte Täler** (Seitenreibung; ohne die Ausnahme drückt er das Alpenrheintal bei Chur auf die Hälfte). Erst damit tut das Bett, was es soll: **Turin und Mailand eisfrei**, Eisvolumen 111 855 → 86 463 km³, Ivrea-Lappen erreicht die Po-Ebene (Eisrand 250 m), Gardalappen −182 m im ausgeschürften Becken, und Chur/Innsbruck/Genf/Killwangen/Schaffhausen bleiben stehen. **Neu 2 — Kalben, und der Befund, dass es nicht greift.** `stauseen()` lief in v2 *nach* dem Zeitschritt und war Anzeige; jetzt läuft es *im* Zeitschritt (alle `kalbJede` Jahre volle Flutung, dazwischen wächst der See dem Eis billig nach) und speist eine Kalbungsrate ∝ Wassertiefe an der Front, gedämpft durch eine **Aufschwimm-Schranke** (ρ_W/ρ_E ≈ 1.12 — ohne sie verhinderte das Kalben schon den *Vorstoss*: die Front lief in jede Pfütze und brach ab, Chur stand im Hochglazial bei 0 m statt 1 519). Dazu kam echte Bathymetrie: ein DTM liefert für einen See den **Wasserspiegel**, nicht den Boden, also fehlte genau die Übertiefung, in die gekalbt wird — der Bake schürft die Zungenbecken jetzt aus 22 handkuratierten Literaturtiefen aus (Bit4 „becken"; Sohlen treffen die Literatur: Bodensee 145 m, Garda −281 m, Como −213 m, Maggiore −179 m). **Ergebnis trotzdem: wirkungslos.** 148 019 km² ohne Kalben gegen 147 981 km² bei der dreifachen Literaturrate — 0.03 %, Chur unverändert 1 518 m; 0.6 / 1.2 / 2.4 und „ohne Aufschwimm-Schranke" liegen alle gleich. Der Grund ist nicht das Gesetz, sondern die Geometrie, und er ist gezählt: **zu jedem Zeitpunkt stehen höchstens ~100 Eisfelder wirklich im Wasser** (−24 000: 11 von 585 Feldern am Wasser eingetaucht; −18 000: 35 von 93; −14 000: keines). Dieses Modell zieht sich zurück, indem Eis *an Ort und Stelle* dünner wird, nicht indem eine Front durch ein Becken wandert — ein Feld ist Eis **oder** See, nie beides, also existiert der Zustand „gegründete Front in tiefem Wasser" nicht, an dem das Gesetz angreifen könnte. Und „Chur liegt 16 000 cal BP noch unter Eis" ist ohnehin **nicht** die Trägheit: das Gleichgewicht sagt dort 148 m, der Lauf 570 m. Was fehlte, wäre eine **Aufsetzlinie** — Eisdicke und Wassertiefe im *selben* Feld. Das ist v4. **Zwei echte Fehler hat das Kalben freigelegt**, beide schon in v2 vorhanden und dort folgenlos, weil die Eisstauseen Kulisse waren: (1) „NIVEAU < 0 = unbesucht" hängte den Priority-Flood auf, sobald eine Zelle unter dem Meeresspiegel liegt (Adriaküste, 1 225 Zellen) — der Heap wuchs bis zum Speicherfehler; ein Vorzeichen ist kein Sentinel, jetzt ein eigenes Besucht-Feld. (2) „Hat ein Niveau" wurde mit „ist ein See" verwechselt — der Flood setzt für *jedes* eisfreie Feld ein Niveau (bei trockenen ist es die eigene Geländehöhe), also galt jede Bergflanke neben einer Talzunge als See mit der Höhendifferenz als Wassertiefe: **35 024 Kalbungsfelder** mit Fronttiefen bis 770 m. Dazu werden Wasserflächen unter `seeMin` Feldern verworfen — bei 1 km/Feld hat das echte Gelände unzählige Ein-Feld-Senken (eine Schlucht ist schmaler als ein Feld und verschwindet im Mittel, der Flood sieht dort einen Riegel): 27 772 „See"-Felder wurden so zu ~7 000 echten in ~140 Flächen. **Gewässerquellen mitgewachsen**: 22 Seen statt 5 (Genfersee — in OSM „Le Léman", nicht „Lac Léman" —, Bourget, Neuenburger-, Thuner-, Brienzer-, Vierwaldstätter-, Maggiore, Lugano, Como, Iseo, Garda, Ammer-, Starnberger, Chiem-, Atter-, Traun-, Wörthersee), je mit Maximaltiefe. **Kosten** (echte Uhr, headless): Lauf 20 s ohne Kalben, 30–34 s mit; 27 Gleichgewichte; Datenfile 3.7 MB. Neue Parameter: `?tauweich=50 ?bettrel=250 ?bettr=8 ?bett=0`, `?profil=0 ?freirel=800`, `?kalb=1.2 ?kalbtief=15 ?kalbjede=250 ?seemin=8 ?kalben=0 ?kalbschwimm=0`, `?bettkarte`, `?kalbkarte`, `?rang=1`; alle v2-Parameter gelten weiter. Offen: Basel trägt noch 43 m Eis (muss 0 sein), und mehrere belegte Maxima werden **nicht** erreicht (Dombes, Villafranca, Memmingen, Salzachlappen, Judenburg, Brianza) — eine einzige τ/ELA-Einstellung trifft nicht alle Lappen gleichzeitig; die Slowenien-Lücke schliesst Copernicus GLO-30 (steht im Register bereit); `world.ts` bleibt nicht georeferenziert. **Leistung gemessen (20. Aug 2026, headless, echte Uhr):** Seitenaufbau 31.6 s (Lauf 30.8 s, alles andere 1.1 s), Heap 325 MB (226 MB Stützstellen), Zeitschritt 24 ms, Eisstausee-Flutung 55 ms, Kartenbild 14 ms. Zwei Bremsen sind Ablauf, nicht Modell: die Flutung läuft je angezeigtem Bild (Abspielen 7.9 statt 52.6 fps), und ein Vorwärtssprung auf der Zeitleiste schreitet den Lauf nach statt die Stützstelle zu laden (9.2 s statt 0.4 s, gleicher Zustand). Zahlen und Gegenprobe in `stromlinien-technik.html`. **Fünf Hebel gezogen (21. Aug 2026), alle gegen einen Fingerabdruck belegt:** neu ist `?pruefe` — nimmt **im Lauf** an neun festen Jahren FNV-1a-Prüfsummen über `H`, `VOR`, `VERWEIL`, `ABTRAG`, `SANDER` plus Fläche/Volumen/Eisrand und legt sie als `window.PRUEFUNG` ab; **alle neun Prüfpunkte sind vorher/nachher identisch**. (1) **Frontüberschuss wandert** (`frontWeiter()`, ab-/zuschaltbar mit `?frontrest=0`): bis dahin nagelte `effFront = min(vFront, hexKm·1000/dt)` die Front auf ein Feld je Schritt, ein feineres Raster kostete damit **k³ statt k²**. Bei 1 km ist `vorStep` exakt 1.0 → Zweig tot, Zustand unverändert; die Gegenprobe fährt `?dt=50` als Stellvertreter für ein feineres Raster: mittlere Abweichung der Eisfläche gegen dt 25 von 0.44 % auf 0.22 %, **im Vorstoss 0.48/0.96/0.58 % → 0.05/0.03/0.08 %**, im Rückzug unverändert (richtig so — der Rückzug ist Dünnerwerden an Ort und Stelle, keine wandernde Front). (2) **Eisstausee-Flutung raus aus dem Zeichenpfad** — `ableiten()` erklärte sie pauschal für ungültig, `zeichne()` holte sie nach, also lief der Priority-Flood **je angezeigtem Bild** (60 ms); jetzt übernimmt die Anzeige die Spiegel des Zeitschritts: **Abspielen 7.9 → 26.6 fps**, Kartenbild 70 → 11.6 ms. (3) **Vorwärtssprung über die Stützstelle**: 8.6 s → 0.4 s bei unveränderter Eisfläche. (4) **Zählsortierung statt Vergleichssort** in `gleichgewicht()` und `fliessbahnen()` (`?zaehlsort=0` schaltet zurück, Gegenprobe: gleiche Prüfsummen): **Gleichgewicht 220 → 152 ms**. (5) **Zeilenschleifen statt `i % COLS`** im Erbe-Durchgang. Pixelvergleich restlos aufgelöst: 0.043 % abweichende Bildpunkte, davon vier Fünftel die (bewusst bis `kalbJede` alten) Seen, der Rest die Uint16-Quantisierung der Stützstelle — dasselbe Jahr durchgeschritten statt gesprungen ist **pixelgleich**. Offen bleiben Stützstellen-Packung (braucht den Worker, weil `CompressionStream` asynchron ist), inkrementelle Flutung und die aktive Menge. **Zeitachse berichtigt (21. Aug 2026): das Labor rechnet in `cal BP`, nicht in v. Chr.** Die Zahlen waren immer BP — belegbar an der eigenen Phasentabelle (Jüngere Dryas 13 000–11 700; belegt 12 900–11 700 *cal BP*, in v. Chr. wären es 10 950–9 750), nur die Beschriftung war falsch. Zahlen unverändert, `?pruefe` bestätigt Zustandsgleichheit an allen neun Prüfpunkten. Die Kampagne bleibt in v. Chr. — dadurch fällt eine **Naht** auf: das Labor endet bei 10 000 cal BP = 8 050 v. Chr., der Kampagnenbeginn steht bei 10 000 v. Chr.; 1 950 Jahre Lücke, Entscheid offen (Handbuch, Prolog-Kapitel). **Rückzug vermessen:** das Zürichseebecken (94 Felder mit heutiger Seefläche) wird bei **16 800 cal BP** frei; bei 18 000 tragen noch 46 der 94 Felder 635 m Eis, die Einzelzelle Seemitte fällt bei 18 575 — Zelle ist nicht Becken. Antrieb ist allein der Erwärmungsast: +760 m in 4 500 a (17 cm/a, steilster Abschnitt 20) und **7 300 Jahre streng monoton**, also keine Stadialgliederung von Bauart; nicht limitierend sind Schmelze (12 m/a räumen 635 m in 53 a) und Kalben (wirkungslos). Widerlegte Erwartung: es entstehen trotzdem **303 Rückzugsmoränen** im Linth-Fenster (Freigabe 19 000–15 000, längste Frontverweildauer 5 350 a) — die Topografie pinnt die Front auch ohne Klimastufen, fehlende Stadiale zeigen sich also als Wälle mit falschem Datum, nicht als fehlende Wälle. Neue Regler `?dehn=1.5` (streckt den Ast ab dem Anker 21 000 cal BP) und `?stadial=17500,800` (Plateau), Sweep: Becken frei 16 800 → 15 900 / 14 900 / 12 875 bei dehn 1.25/1.5/2, **Maximum in allen Varianten unverändert** (170 912 km², 5/11 Marken) — der Preis steht bei Chur, das von 15 925 auf 13 400 rutscht und damit schlechter wird; Killwangen bewegt sich selbst bei dehn 2 nur 350 a. Gleichmässige Streckung reicht also nicht, es braucht gestufte Stadiale an belegten Daten. **Gegen die swisstopo-LGM-Karte flächig geprüft** (GK500-LGM 1:500 000, Bini et al. 2009, `pipeline/data/data.zip`, © swisstopo): Georeferenz aus den vier Eckpunkten der Metadaten = exaktes LV03-Rechteck E 482 200–835 700 / N 63 000–301 000 bei genau 50,00 m/Pixel; Klassierung über luminanznormierte Chromatizität (zweigipflig, leere Lücke bei u ≈ −0,02, Ergebnis über die ganze Lücke stabil 82–83 %). Auf 91 397 km²: **82,9 % Übereinstimmung**, Modellfläche **+4,8 %** — aber 15 600 km² am falschen Ort. Regional: Zürich/Linth 94,5 %, Wallis 94,5 %, Graubünden 93,8 %, dagegen mittleres Mittelland **47,9 %** (3 365 km² zu weit, 0 zu kurz), Südseite/Po-Vorland 65,1 % (3 783 zu weit), Bodensee/Allgäu 61,4 % (2 081 zu kurz). Das Übermass ist **kein Randfilm**: Median 645 m Eisdicke, dicker als das richtig platzierte Eis (543 m). Methodisch der wichtigste Punkt: die △-Marken prüfen nur, ob das Eis bis zur Marke reicht, nie ob es dort aufhört — Wangen a. d. Aare ist ein Punkt-Treffer und liegt mitten im Fenster mit 47,9 % Übereinstimmung. Datenfile: `eiszeit-labor-v3.data.js` (`pipeline/fetch/bake-eiszeit.mjs`). |
| `eiszeit-labor-v2.html` | `archive/` | superseded | Durch v3 ersetzt (ganzer Alpenbogen, Bett, Kalben). Datenfile mit archiviert. Der Rest dieser Zeile ist die Entstehungsgeschichte von v2 und bleibt stehen.  **Das Ende der Eiszeit als Lauf durch die Zeit — mit Gedächtnis und Erbe.** v1 rechnete pro Jahr ein Gleichgewicht aus der Schneegrenze (`rechne(ela)` war eine *reine Funktion* der ELA): bei gleicher Schneegrenze sahen Vorstoss und Rückzug identisch aus, es gab keine Trägheit, kein totes Eis, und nichts, was das Eis hinterlässt. v2 macht daraus einen **Lauf**: das Gleichgewicht ist nur noch der Baustein (Zielzustand), die Eisdicke wandert in 25-Jahr-Schritten mit **begrenzten Raten** darauf zu — die Front kriecht (≤ 40 m/a, ein Feld je Zeitschritt), die Dicke wächst ≤ 1.5 m/a, und zurück geht es nur mit der **Schmelzrate am Ort** (aus der Massenbilanz, gedeckelt). Damit die 1200 Zeitschritte nicht 1200 Dijkstra-Läufe kosten, liegen die Gleichgewichte auf einer **Leiter** von 50-m-Schneegrenzen (~27 echte Gleichgewichte, dazwischen linear verrechnet); **Stützstellen** alle 500 Jahre (61 Frames à Dicke/Verweildauer/Schürfung/Schotter, ~9 MB) machen Zurückspulen und Ziehen an der Zeitleiste billig — vorwärts wird nur weitergeschritten. **Gemessen** (headless, SwiftShader, worst case): der ganze Lauf kostet gegenüber v1 rund **2 s Ladezeit** (v1 2.2 s, v2 4.3 s, je drei Läufe); Einzelzeiten sind unter `--virtual-time-budget` nicht messbar (virtuelle Uhr → 0.0 ms), `?bench` gilt nur im echten Browser. **Hysterese ist damit nachweisbar** und wird angezeigt („Nachhang" = Fläche gegen Gleichgewichtsfläche): beim Wiedervorstoss hinkt das Eis nach (Egesen −4 %), beim Rückzug bleibt es liegen (+1…+4 %, „totes Eis" als eigene Kennzahl), und bei *gleicher* Schneegrenze trägt der Rückzug mehr Eis als der Vorstoss (ELA 2400/2420: 3 122 gegen 2 887 km²). **Das Erbe** wird im Lauf mitgeschrieben und ist die eigentliche Ausbeute: **Endmoränen** aus der Verweildauer der Front (nur Zungen zählen — Front im Zehrgebiet, ≥ 60 m Eis; ohne diese Bedingung war jeder Firnrand um jeden Gipfel Moräne, 10 208 Felder = ein Drittel der Karte), **Schürfung** (Dicke × Oberflächengefälle × Zeit, bewusst nur als Karte — das DTM ist die Landschaft *nach* der Eiszeit, ein Zurückschreiben zählte sie doppelt), **Schotterflur** vor der Front, **Eisstauseen** (Priority-Flood über die Eisfreien: Wasser entkommt übers Gelände zum Kartenrand oder über die Eisoberfläche, der niedrigste Weg gewinnt), **Seen erst nach der Freigabe** (Zürich-/Walen-/Bodensee entstehen im Lauf, statt Kulisse zu sein) und **Findlinge** — 150 Partikel aus fünf echten Leitgesteinen (Julier-Granit, Bündnerschiefer, Verrucano, Tödi-Granit, Silvretta), die dem Oberflächengefälle folgen und dort liegen bleiben, wo das Eis am Ende zerfiel (129 abgelegt; Ruhelage wird überschrieben, solange die Front sie wieder mitnimmt). Dazu **Freigabejahr je Feld** — „eisfrei ab?" ist damit ein Nachschlagen statt 61 Modellläufen wie in v1. Neu: **Erbe-Ansicht** (`?erbe`, Eis aus, Hinterlassenschaften an), vier neue Plättchen-Marken (Endmoräne, Schotterflur, Findling, Eisstausee) in der Vorschau, Eisfläche-gegen-Gleichgewicht als zweite Kurve auf der Zeitleiste, **Export der Endlage** als `startkarte-10000vChr.json` (Gelände als base64-Uint8 wie die Pipeline-Dateien + Fundstellen-Kandidaten mit lon/lat: Findlinge mit Herkunftsgestein, die 40 stärksten Moränenwälle). **Drei Modellfehler, die erst das Gedächtnis sichtbar machte** (alle korrigiert): (1) jeder Hügel über der Schneegrenze nukleierte sofort mit der *regionalen* Gleichgewichtsdicke — die Front war überflüssig, Fronttempo 40 und 5 m/a ergaben dieselbe Eisfläche; jetzt bekommt ein Feld ohne Anschluss nur ein eigenes Firnfeld. (2) Der Deckel der Eisoberfläche stieg in v1 mit der halben Rate der Schneegrenze — ein *wärmeres* Klima erlaubte lokal mehr Eis, der Überschuss rastete ein (ein Talfeld bei Chur trug bei ELA 1800 dasselbe Eis wie im Hochglazial und stand 10 000 cal BP noch unter 1 533 statt 464 m Eis); der Deckel ist jetzt fest (bei der Hochglazial-ELA unverändert, die Kalibrierung hängt an dieser Sprosse). Zusätzlich werden die Leitersprossen **monoton geklemmt** (wärmer nie mehr Eis als kälter, Anker = kälteste Sprosse der Kurve; ein erfundener kälterer Anker bei ELA 800 zog die kalibrierte Maximallage auf 17 847 km² herunter). (3) Die Egesen-Delle der Klimakurve lag bei 2 120 m und zeitlich *nach* dem Ende der Jüngeren Dryas — sie schaltete die Talzunge wieder ein und füllte das Rheintal 1 000 Jahre vor Spielbeginn mit 1 661 m Eis; korrigiert auf 2 280 m im Egesen-Fenster (belegte Egesen-ELA liegt 500–700 m unter der heutigen). **Kalibrierung erhalten**: tiefster Eisrand im Hochglazial 420 m ü. M. (v1: 421), Front an den belegten △-Marken, Maximum liegt beim Hochglazial (19 592 km²; die *Flächenzahl* ist nicht mit v1 vergleichbar — v2 zählt genau die gezeichneten Eisfelder, v1 zählte nur die vom Dijkstra besuchten: eigenständige Firnfelder fehlten, hauchdünner Randabfall zählte mit), und **bei 10 000 cal BP steht das Tal offen** (2 192 km² Resteis, Chur eisfrei — in v1 lagen dort noch 464 m). **Zur DTM-Lücke** (6 220 von 28 576 Feldern geschätzt): die Endlage selbst trägt sie — nur 12 % des Resteises liegen auf geschätzten Feldern (253 von 2 192 km²), das Rheintal ist frei. Auf erfundenem Gelände steht dagegen die **kalibrierte Maximalausdehnung**: mit `?nurecht` wirken die geschätzten Felder als Wand und der Rheingletscher erreicht Schaffhausen nicht mehr (Eisrand 479 statt 421 m, 11 400 statt 19 592 km²) — die Fliesswege laufen entlang der Landesgrenze. Die Lücke zu schliessen ist damit Voraussetzung dafür, dass die Kalibrierung auf gemessenem Gelände steht. Offen: Zuordnung auf das gestaltete 22×44-Weltraster (`world.ts` ist nicht georeferenziert), Eisdicken im Vorland weiter zu hoch, Talzungen halten sich zu lange (Chur bei 16 000 cal BP noch unter 1 662 m Eis). Debug: `?jahr=-23000`, `?endlage`, `?erbe`, `?traegheit=0` (= v1-Verhalten), `?dt=50`, `?vfront=15`, `?vwachs`, `?ablmin`, `?key=500`, `?mor=1200`, `?moraene=0`, `?abtrag`, `?sander=0`, `?findlinge=0`, `?stausee=0`, `?seenspaet=0`, `?play`/`?vor`/`?rueck`, `?tempo`, `?loop`, `?ela=1500`, alle v1-Modellregler, `?nurecht`, `?stroeme`, `?feld=9.53,46.85`, `?lon/lat/z`, `?panel=0`, `?stats`, `?bench`. **Renderer (20. Aug 2026):** zwei Betriebsarten — unter 8 px Hexradius ein **Zellbild** (eine Zelle = zwei Bildpunkte einer Bitmap in Zellkoordinaten, ein `drawImage`; der Halbzellen-Versatz der odd-r-Zeilen ist dort genau ein Bildpunkt), darüber die gewohnten **Hexpfade**; Linien (Eisrand, Ströme, Nunatakker, Findlinge, Landmarken) bleiben in beiden Vektor. Gemessen mit echter Uhr (headless, Software-Rendering, dpr 1, 1278×748, Vollansicht): warmes Bild **392 → 2.0 ms**, kaltes (jeder Zoom-/Pan-Schritt) **1 066 → 2.0 ms**, Abspielbild samt Modellschritt und HUD **255 → 7.9 ms**, Erbe-Ansicht 81 → 1.0 ms, Gelände-Basis 894 → 0.2 ms. Dazu: Farben liegen doppelt in Tabellen (CSS für den Vektor, gepackte RGBA fürs Zellbild) aus *einer* Rechnung, Punkt-Sammler mit stehenden Puffern statt Zeichenketten-Map, Schraffur als `createPattern` statt `clip()` über 6 220 Hexe (345 ms). Der Vektorpfad profitiert mit: 392 → 273 ms in der Vollansicht, 9 ms hineingezoomt. Neue Parameter: `?raster=auto|bild|hex`, `?rasterab=8`, `?dpr=1`. Ausführlich in `stromlinien-technik.html`. **Höhen** kommen jetzt aus dem Register `pipeline/sources/hoehen.manifest.json` (mehrere DTMs mit Vorrang, blockweise gelesen und beim Lesen ausgedünnt); je Feld steht die Quelle im Datenfile (`quelle` + `hoehenquellen`), und der Umbau hat die gebackenen Höhen Wert für Wert nicht verändert. **DTM-Lücke geschlossen** (20. Aug 2026): echte Sonny-DTMs für AT/DE/IT dazu (LI lag bereits im Schweizer Raster) — von 28 576 Feldern nur noch 10 ungedeckt, statt 6 220 (21.8 %). Das brach die Kalibrierung: mit unveränderten Parametern erreichte das Eis Schaffhausen und Killwangen nicht mehr (Fläche 19 592 → 10 950 km², Eisrand 420 → 523 m) — die geschätzte Füllung war glatter als das echte Alpenrelief im Nährgebiet und trug mehr Nachschub in die Zunge, als real ankommt. Neu kalibriert (Fliessgrenze τ 140 → 98 kPa, Sweep über 24 headless-Läufe mit echter Uhr): Eisrand wieder exakt 420 m, beide Marken mit Abstand erreicht (nicht auf der Kippe), 98 kPa näher an publizierten Werten für Talgletscher als die alten 140. `?nurecht` bestätigt den Fortschritt direkt: die verbliebenen 10 geschätzten Felder ändern die Fläche nur noch um 0.03 %, vorher 42 %. Resteis bei 10 000 cal BP jetzt 1 543 km² (vorher 2 192 — die Zahl war an die alte Kalibrierung gebunden, keine externe Marke); Chur bleibt eisfrei. Datenfile: `eiszeit-labor-v2.data.js`. |
| `eiszeit-labor-v1.html` | `archive/` | superseded | Erste Fassung: **Ende der letzten Eiszeit als Animation** — 40 000 bis 10 000 cal BP auf echtem Gelände (Sonny-DTM Schweiz 50 m, 1 km/Hex, 152×188 Felder über 7.95–10 °O / 46.4–47.85 °N), neue Plättchen-Art „Eis" (Dicke → Helligkeit, Firn, Eisrand als Kantenmarke, Nunatak), Klimakurve mit acht benannten Phasen und daraus ein Eisfluss-Modell in vier Schritten (Max-First-Dijkstra mit plastischem Fliessgesetz und Zehrung → Massenbilanz → Randabfall → Firnfelder), 11 Modellregler, Feld-Inspektor mit „eisfrei ab?". Kalibriert am Kartenbild (Maximalausdehnung Schaffhausen/Killwangen, Randen frei, Säntis und Tödi als Fels). **Der Zustand war eine reine Funktion der Schneegrenze** — dadurch keine Trägheit, kein totes Eis, kein Erbe, und Vorstoss = Rückzug bei gleicher ELA. Durch v2 ersetzt (Lauf mit Gedächtnis + Erbe); mit `?traegheit=0` ist das v1-Verhalten in v2 weiterhin erreichbar. Datenfile mit archiviert. |
| `rhein-gesamt-v1.html` | `drafts/` | active | Der **gesamte Rhein** vom Quellgebiet (Tomasee/Vorderrhein + Paradiesgletscher/Hinterrhein) bis zur Nordseemündung als eine Hexkarte (40×64, pointy-top, odd-r wie `grid.ts`). Nicht handgezeichnet: die Welt wird aus **Wegpunktlisten pro Abschnitt** generiert (Hex-Linien via cube lerp), Bergzonen grob als Rechtecke, Flusstäler werden freigeschnitten, Ufer abgeleitet. Sieben farbige Abschnitte (Alpenrhein · Bodensee · Hochrhein · Oberrhein · Mittelrhein · Niederrhein · Deltarhein), Nebenläufe als Stummel (Hinterrhein, Lek, Aare, Neckar, Main, Mosel, Ruhr), 16 Landmarken, ≈km-Skala ab Quelle (linear auf 1230 km, v1-Näherung). UI: Legende-Chips (Highlight + Scroll), Feld-Inspektor, km-Marken, Zoom, Demo „Den Fluss entlang" (Marke wandert Quelle→Meer). Geographie stark vereinfacht (v1). Debug: `?abschnitt=alpen\|bodensee\|hoch\|ober\|mittel\|nieder\|delta`, `?km`, `?labels=0`, `?demo`, `?hexw=13`, `?inspect=c,r`. |
| `asset-editor-v1.html` | `drafts/` | active | **Terrain-Asset-Editor mit integrierter Pipeline** — Antwort auf die Frage, wie Terrain-Tiles als Assets statt als Code-Farbwerte veröffentlicht werden, ohne die Editierbarkeit zu verlieren. Quelle sind einzelne SVG-Dateien (`pipeline/assets/terrain/<kollektion>/{flach,ufer,hang,water,lake,wald,berg,eis}.svg` — die letzten drei noch **ohne Entsprechung** in `app/src/stromlinien/types.ts`, ein Vorgriff auf künftige Terrain-Arten, siehe unten) — im Explorer sichtbar, mit jedem Text-/Vektoreditor bearbeitbar, git-diffbar. **Sieben Kollektionen zum Durchprobieren** (je ein Ordner, unabhängig editierbar): vier Stilrichtungen — `pastell` (weich, leichte Texturakzente), `kontrast` (kräftige Flächen, grafische Muster), `aquarell` (gemalte, überlappende Flecken statt Muster), `linien` (minimales Karten-/Blueprint-Liniendesign auf Pergamentgrund) — und drei **Epochen-Kollektionen**, die dieselben acht Arten über die Zeit erzählen: `palaeolithisch` (Eiszeit — Eis dominiert, Wald nur spärliche Tundra-Reste), `neolithisch` (Ackerbau — Furchen statt Grasbüschel auf Flachland, Terrassen am Hang, Eis im Rückzug), `holozaen` (heute — dichter Wald, kräftiges Grün, Eis nur noch als kleiner alpiner Rest in den Bergspitzen). Drei Spalten: **Galerie** (Live-Thumbnails der gewählten Kollektion von der Platte über `pipeline/asset-server.mjs`, Kollektions-Umschalter oben), **Editor** (Rohquelle + Live-Vorschau, unabhängig von der Karte — Speichern schreibt nur die Einzeldatei), **Karte** (eigener Kollektions-Umschalter — rendert sofort jede bereits **veröffentlichte** Kollektion aus dem Bundle `terrain-assets.bundle.js`, ohne erneut zu backen; nur *neue* Änderungen brauchen „Veröffentlichen"). „Veröffentlichen" sichert offene Änderungen, stösst den Bake an (bündelt **alle** Kollektionen auf einmal, NDJSON-Log wie bei `rhein-tiles-v4`) und lädt das Bundle neu — erst dann übernimmt die Karte den neuen Stand, editieren allein genügt nicht. Zwei Renderpfade zum Vergleich: **gebündelt** (Bitmap-Cache pro Terrain-Art/Kollektion, einmal gerastert, `drawImage` ohne Pfad/Clip pro Feld) gegen **geklippt pro Feld** (dieselbe Bitmap, aber mit frisch aufgebautem Hexpfad + `clip()` bei jedem Zeichnen) — Knopf „Benchmark" misst beide mit echter Uhr über N Kartenbilder und zeigt den Faktor (im echten Browser; unter `--virtual-time-budget` steht die Uhr wie in `stromlinien-technik.html` beschrieben). Ohne Server (`file://`) bleibt die Karte aus dem zuletzt gebackenen Bundle lesbar, Editor/Speichern/Veröffentlichen sind deaktiviert (gleiches Fallback-Muster wie `rhein-tiles-v4`). Debug: `?collection=<name>` (Editor-Kollektion), `?mapCollection=<name>` (Karten-Kollektion), `?tile=<key>`, `?hexw=`, `?mode=vector`, `?bench`. |
| `rhein-tiles-v4.html` | `drafts/` | active | **Tileset-Viewer v4** — alles aus v3 (s.u.), dazu die **Steuerung der Pipeline aus dem Viewer heraus**: erster Kandidat für ein Werkzeug, das die Map-Designer:innen selbst bedienen. **▭ Gebiet ziehen** schaltet die Karte in einen Ziehmodus (Geste spannt eine Box auf statt zu schwenken); die Box wird grün mit **Kantenlängen in km und geschätzter Rasterpunktzahl** angeschrieben, die bestehende Höhen-Region liegt zum Vergleich grau gestrichelt darunter — man sieht also, was man ersetzt. **Pipeline starten** schickt die Box an `pipeline/serve.mjs` (nur `127.0.0.1`, startet genau die zwei bekannten Skripte), der `fetch/normalize-dtm.mjs --lonW=… ` und danach `bake.mjs` laufen lässt und deren Ausgabe zeilenweise als NDJSON zurückstreamt. Ein **modaler Dialog** zeigt Stufenliste (○/◐/●/✕), Fortschrittsbalken, das mitlaufende Log und am Ende „Tileset neu laden". Kernentscheidung dahinter: die Region steht ab jetzt **nur noch in der Höhenquelle** — `bake.mjs` leitet die Region der DTM-Ebene aus den vorhandenen `*.grid.json` ab, statt dieselbe Box ein zweites Mal zu führen; die Höhenquelle heisst deshalb regionsneutral (`sonny-dtm-ch50.grid.json`) und wird pro Lauf **ersetzt**. Ohne laufenden Server bleibt der Prototyp per Doppelklick voll nutzbar: Statusanzeige „file://", Startknopf gesperrt, Hinweis wie man den Server startet — Ziehen und Vermessen gehen trotzdem. Fehlerfälle sind echte Pfade, keine Attrappen: Gebiet ausserhalb der Schweiz (das DTM deckt nur CH ab) bricht mit klarer Meldung ab und lässt die bisherige Höhenquelle unangetastet, zu grosse Gebiete (> 4 Mio. Rasterpunkte) sperrt schon der Viewer. Debug: `?bbox=lonW,latS,lonE,latN` (Gebiet vorbelegen), `?draw` (Ziehmodus an), `?pipeline=lauf\|fertig\|fehler` (Dialog als Schaubild ohne echten Lauf) — dazu alle v3-Parameter. Datenfile: `rhein-tiles-v4.data.js`. |
| `rhein-tiles-v3.html` | `archive/` | superseded | **Tileset-Viewer v3** — v2-Rendering (Scene-Canvas-Blitting, Farb-Batches, s.u.) plus ein linkes **Baum-Panel**, das die Pipeline selbst sichtbar macht: **Pipeline** (die vier Stufen als Legende), **Quellen** (jede `pipeline/sources/*.json`-Datei aus `TS.provenance` — Dateiname, Quelle/Lizenz-Badge, Feature-Anzahl/-Arten bzw. „Höhenraster") **Ebenen** (jede Tileset-Ebene — Auflösung, Zellen, Kacheln, global/regional) und **Landmarken** (jeder Punkt aus `TS.landmarks`, mit „nur Ebene 1/2"-Flag). Anklicken hebt die Bounding-Box (Quelle/Ebene) bzw. einen Ring um den Punkt (Landmarke) auf der Karte hervor und fliegt die Kamera hin; ein zweiter Klick hebt die Auswahl wieder auf. `bake.mjs` liefert dafür pro Quelle `bbox` (lon/lat) und `bboxKm` (fertig projiziert) mit, Ebenen nutzen ihr vorhandenes `regionKm`. Dazu ein **Terrain-Filter**: Checkbox pro Terrain-Art (Flachland/Ufer/Bergland/Fluss/See/Nordsee/Nebenlauf/Hang, mit Farb-Swatch aus `TERRCOL`) blendet deren Kacheln direkt im Scene-Refill aus (`hiddenTerr`-Set) — z. B. Bergland+Hang aus, um nur noch Wasserläufe/Flachland zu sehen. Landmarken-Sichtbarkeit einzeln abschaltbar (Checkbox „auf der Karte anzeigen", ersetzt das alte reine `?labels=0`). Panel ein-/ausblendbar (`?panel=0` startet zugeklappt). Debug: `?select=source:<file>|level:<id>|landmark:<index>` wählt beim Laden bereits aus, `?hide=Bergland,Hang` (Namen oder Indizes) blendet Terrain-Arten schon beim Start aus — beides ohne Klick, für Screenshots. Datenfile heisst jetzt `rhein-tiles-v3.data.js` (mit der Umbenennung von `bake.mjs` mitgezogen). Rest wie v2 (s.u.), unverändert. Durch v4 ersetzt (Gebiet ziehen + Pipeline aus dem Viewer starten). |
| `rhein-tiles-v2.html` | `archive/` | superseded | **Tileset-Viewer v2** — Konsument der Karten-Pipeline (`pipeline/`, siehe deren README): Quellenverzeichnis mit normalisierten WGS84-Geodaten (`pipeline/sources/`, mit Provenienz/Lizenz) → `node pipeline/bake.mjs` schreibt das Tileset als **`rhein-tiles-v2.data.js`** (generiert, nicht von Hand editieren — bewusste Ausnahme von der Ein-Datei-Regel, damit der Viewer per file:// läuft). **Neu in v2 — abgeleitete Ebenen:** das Flächen-Terrain wird nur noch einmal auf der feinsten Ebene gerastert (Basisraster = Ebene 2: Polygone + DTM) und Ebene 1/0 daraus aggregiert (Zellzentren-Zuordnung: Mehrheit fürs Terrain, Mittel für die Höhe; Linien wie der Flusslauf weiterhin pro Ebene, sonst überleben sie den Mehrheitsentscheid nicht) — damit erbt Ebene 1/0 das echte DTM-Relief (`pipeline/verify-cell.mjs` prüft Einzelzellen samt Basis-Verteilung). **Neu in v2 — Viewer-Performance:** Scene-Canvas (Schwenken = reines drawImage-Blitting), rAF-Koaleszenz, Farb-Batching (fertig gemischte Farben pro Terrain×Höhenstufe×Abschnitt, ~25 fill()-Aufrufe statt Tausender Stilwechsel), zweistufiger Refill (klein bei Eingabe, voller Rand im Idle). Gemessen headless (SwiftShader, worst case, `?bench`): Schwenken Ø ~4 ms, Zoom Ø ~33 ms (vorher 184/395 ms). Format: 32×32-Kacheln mit Terrain- und Abschnitts-Layer als base64-Uint8Array, leere Kacheln entfallen; Flusspfad mit echten km (Haversine), Landmarken mit `detail`-Flag. Viewer: Canvas mit Viewport-Culling, Lazy-Tile-Decode + Cache, Ebenenwechsel am Zoom, Pan/Zoom/Fit, Feld-Inspektor, Stats-Overlay (sichtbare Zellen / dekodierte Kacheln). Täler werden beim Bake freigeschnitten — das Mittelrhein-Durchbruchstal entsteht aus Bergland-Polygonen + Flusslinie von selbst. **Ebene 2 (0.4 km/Hex, Region Alpenrhein)** kommt aus dem echten Sonny-DTM (50 m, CC BY 4.0): `pipeline/fetch/normalize-dtm.mjs` (WGS84→UTM, bilineares 200-m-Raster) → Bake klassifiziert Steigung/Relief zu flach/**Hang**/Berg (Schwellen in `CLS`) + Höhen-Layer (Schummerung, „m ü. M." im Inspektor); Ebenen mit Region liefern nur Kacheln darin, der Viewer schaltet nur innerhalb um. Canvas zeichnet pointy-top (Ecke oben/unten). **Echte OSM-Geometrie** (Overpass, ODbL): der **Hauptlauf selbst** ist die OSM-Relation „Rhein" (inkl. Vorderrhein ab Tomasee; Waal-Route bis zum Haringvliet, dazu der Lek-Arm bis Hoek van Holland; Wege entlang einer Referenzlinie geordnet, Parallelkanäle verworfen; ~1265 km statt der 1050 der Hand-Polylinie), dazu Aare, Reuss, Limmat, Seez, Linth, Sihl als Nebenläufe und Bodensee (inkl. Untersee), Walensee, Zürichsee, Obersee, Sihlsee als echte See-Polygone. Sehenswert: das Wasserschloss bei Brugg (`?lon=8.28&lat=47.42&z=11`), die Linth-Seen-Kette (`?lon=8.90&lat=47.20&z=9`), das Delta mit zwei Armen (`?lon=4.9&lat=51.85&z=9`). Beim Vergröbern gewinnen Seen schon ab 25 % Anteil (Generalisierung), damit Zürich- und Walensee auf Ebene 0 sichtbar bleiben. Debug: `?stats`, `?bench` (synchroner Messlauf, Ergebnis im HUD), `?level=0\|1\|2`, `?lon=9.50&lat=46.90&z=25` (Ebene 2 bei Chur), `?inspect=lon,lat`, `?labels=0`. |
| `rhein-tiles-v1.html` | `archive/` | superseded | Erste Fassung des Tileset-Viewers (mit `rhein-tiles-v1.data.js`): Ebenen unabhängig voneinander gerastert (Ebene 1/0 wussten nichts vom DTM der Ebene 2), Vollbild-Neuzeichnung bei jedem Input-Event. Durch v2 ersetzt (abgeleitete Ebenen + Blit-Rendering). |
| `spielfeld-entlastung-v1.html` | `drafts/` | active | HUD-Entlastung im laufenden Spiel: kompakte Punkt-Timeline (Tap für Runden-Info), Ressourcen-Chips mit Delta-Pop-Animation, einklappbares Lager-Sammelpanel (aktive Boni, Werkzeug-Bau), Chronik als Bottom-Sheet (sammelt Toast-Historie), Feld-Inspektor als eigenes Bottom-Sheet (schiebt die Karte nicht). Debug: `?chronik`, `?sheet`, `?lager`, `?delta`, `?demo`. |
